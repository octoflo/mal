| A program to reverse a given list ex. a, b, c -> c, b, a
| Used to show the difference between the same program in LISP and Axolotl

| Axolotl
(function reverse (lst)
  (if
   ((none? lst) '())
   ((rev (rest lst)) + (list (first lst)))))
